import React, { Children } from "react";

const Verse = ({children}) => {
    return (

            <div className="font-gabarito text-default font-extralight text-center whitespace-pre-line">
                {children}
            </div>
  
    )
}

export default Verse
